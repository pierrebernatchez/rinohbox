This comes out
=============

-p PREAMBLE, --preamble PREAMBLE
                        File containing preamble text for index.rst file (dflt
                        None)

This comes in
=============

-f FRONTPAGE, --frontpage FRONTPAGE
                        File similar to any other content file, except it is intended to be the
			first page of multifile documentscontaining preamble text for index.rst file
			(dflt None)

I need to change setstage as follows:

We will just emit an index.rst file with no thing but its toc entry.
So everything about preamble comes out completely.

All articles will be treated equally, and if we have a front page, it will be TOO.
A front page is rendered just like any article, only it is the first thing
in contents.

So we drop the preamble argument and we introduce the frontpage argument.

Frontpage needs the same sort of treatment to make sure it is excluded tom valid args,
just as preamble was. Must not be index.rst, must not be duplicated in the article list.


